Good news and bad news. The SDK is initializing now — we're past the zero-activity problem from previous builds. Here's what actually happened this session:
Image injection said "inject unsuccessfully" — this is the first time we've seen that message at all. Previous builds had zero SDK activity so no result was ever reported. The fact that the portal is now reporting a result means configure() is running and the handshake is being attempted. The failure is happening at the connection stage, not initialization. What port/endpoint does the injection client try to connect to, and is there any logging we can add to show whether that connection is succeeding or timing out?
The app auto-logged in after install without me doing anything — I didn't trigger biometrics manually. The app just logged me straight in. The log shows the Kobiton test runner sending keyboard and touch events to KobitonExpenseTracker at 13:06:11 immediately after install. It looks like a biometric pass signal fired automatically on install rather than waiting for a manual trigger. The demo needs to stay on the login screen and wait for me to trigger biometrics from the Kobiton portal — it shouldn't auto-log in on its own.
Camera is running at full 30fps this session — previous broken sessions showed the app camera throttled to ~14fps, which confirmed SDK frame interception was happening. This session the camera is running at native 30fps and there's no fps log line from the KobitonExpenseTracker process specifically. That suggests the image injection pipeline may not be intercepting the camera feed in this build. If the SDK isn't intercepting frames, injection will always fail regardless of the handshake.
Three things needed: (1) fix the auto-login — the app must stay on the login screen until biometrics are triggered manually from the portal, (2) add logging to show whether the injection client's outbound connection is succeeding or failing and to what endpoint, (3) check whether the camera interception/frame throttling is supposed to be present and why it's missing from this build.